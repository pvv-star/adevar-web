import LoginClient from './LoginClient';

export const metadata = {
  title: 'Autentificare — adevar.ai',
  description: 'Conectati-va la contul dvs. adevar.ai.',
};

export default function LoginPage() {
  return <LoginClient />;
}
